{
  "name": "@subsystemio/subsystem-image",
  "version": "0.2.0",
  "description": "Turn a subsystem into a flashable Raspberry Pi kiosk card — no Node, npm or network on the device",
  "bin": {
    "subsystem-image": "bin/subsystem-image.js"
  },
  "files": [
    "bin",
    "build-payload.sh",
    "prepare-sd.sh",
    "mcp-card.sh",
    "Automation_Custom_Script.sh"
  ],
  "scripts": {
    "lint": "prettier --check .",
    "format": "prettier --write .",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/subsystemio/subsystem-image.git"
  },
  "author": "subsystem.io",
  "license": "MIT",
  "dependencies": {
    "bare-env": "^3.0.1",
    "bare-fs": "^4.7.4",
    "bare-path": "^3.1.1",
    "bare-subprocess": "^6.1.0",
    "paparam": "^1.10.2"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-config-holepunch": "^2.0.0"
  }
}
